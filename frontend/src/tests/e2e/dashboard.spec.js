/**
 * End-to-end tests for the dashboard.
 * Requires both backend and frontend running.
 */
import { test, expect } from '@playwright/test'

// TODO: implement tests as part of TDD workflow

// test('dashboard loads and shows a weather card', async ({ page }) => {
//   await page.goto('http://localhost:5173')
//   await expect(page.getByText('My Dashboard')).toBeVisible()
//   await expect(page.locator('.weather-card')).toBeVisible()
// })
